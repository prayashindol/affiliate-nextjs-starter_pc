# Development (local)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional server-side alias used in server code if you prefer
# SITE_URL=http://localhost:3000

# NextAuth Configuration (REQUIRED)
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here

# Sanity Configuration (REQUIRED for content management)
# Client-side variable (available in browser) - must start with NEXT_PUBLIC_
# Get this from: Sanity Dashboard → API → Tokens → Create new token
# Should start with 'sk' for read/write tokens
NEXT_PUBLIC_SANITY_TOKEN=your-sanity-api-token

# Viator API Configuration (REQUIRED for tour integration)
# Server-side variable (secure, not available in browser) - no NEXT_PUBLIC_ prefix
# Get this from: Viator Partner API Dashboard
VIATOR_API_KEY=your-viator-api-key

# Production
# NEXT_PUBLIC_SITE_URL=https://strspecialist.com
# SITE_URL=https://strspecialist.com

# ================================
# VERCEL DEPLOYMENT INSTRUCTIONS
# ================================
# 
# To set these environment variables in Vercel:
# 
# 1. Go to: https://vercel.com/dashboard
# 2. Select your project
# 3. Go to Settings → Environment Variables
# 4. Add each variable above with these exact names:
#    - NEXTAUTH_SECRET (for all environments)
#    - NEXT_PUBLIC_SANITY_TOKEN (for all environments) 
#    - VIATOR_API_KEY (for all environments)
#    - NEXT_PUBLIC_SITE_URL (set to your domain for production)
# 
# IMPORTANT:
# - Set variables for Production, Preview, AND Development environments
# - After adding variables, you MUST redeploy your application
# - Use the diagnostics page at /test/env-diagnostics to verify setup
# 
# Variable Types:
# - NEXT_PUBLIC_* = Available in browser (client-side)
# - Others = Server-only (secure, not exposed to browser)
#